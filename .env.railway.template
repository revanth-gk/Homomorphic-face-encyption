# Railway Environment Variables Template
# Copy these variables to your Railway service settings

# =============================================================================
# BACKEND SERVICE VARIABLES
# =============================================================================

# Database (Railway provides these via references)
# Use Railway's variable reference syntax: ${{Postgres.VARIABLE_NAME}}
DB_HOST=${{Postgres.PGHOST}}
DB_PORT=${{Postgres.PGPORT}}
DB_USER=${{Postgres.PGUSER}}
DB_PASSWORD=${{Postgres.PGPASSWORD}}
DB_NAME=${{Postgres.PGDATABASE}}

# Application Secrets (GENERATE UNIQUE VALUES!)
# Use: [guid]::NewGuid().ToString().Replace("-","")
SECRET_KEY=your-32-character-secure-random-string
JWT_SECRET=your-32-character-secure-random-string
DB_ENCRYPTION_KEY=your-32-character-encryption-key

# Flask Configuration
FLASK_ENV=production
PYTHONPATH=/app/src

# CORS Origins (update with your frontend Railway URL)
CORS_ORIGINS=https://your-frontend-xxx.railway.app

# Redis (optional - add Redis service and reference it)
# REDIS_URL=${{Redis.REDIS_URL}}

# =============================================================================
# FRONTEND SERVICE VARIABLES
# =============================================================================

# API URL (your backend Railway URL)
VITE_API_URL=https://your-backend-xxx.railway.app
